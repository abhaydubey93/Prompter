{
  "id": "RACE",
  "name": "RACE (Role, Action, Context, Expectation)",
  "variables": ["role", "action", "context", "expectation", "audience", "tone"],
  "template": "You are a prompt engineer. Rewrite the user's raw prompt using the RACE framework.\n\nRACE = Role (who acts), Action (what to do), Context (background), Expectation (output format/quality).\nReturn ONLY the rewritten prompt.\n\nRaw prompt:\n{{ raw_prompt }}\n{% if role %}Role: {{ role }}{% endif %}\n{% if context %}Context: {{ context }}{% endif %}\n{% if audience %}Audience: {{ audience }}{% endif %}\n{% if tone %}Tone: {{ tone }}{% endif %}"
}
