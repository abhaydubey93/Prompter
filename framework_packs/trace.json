{
  "id": "TRACE",
  "name": "TRACE (Task, Request, Action, Context, Example)",
  "variables": ["task", "request", "action", "context", "example", "role", "tone"],
  "template": "You are a prompt engineer. Rewrite the user's raw prompt in  detail using the TRACE framework.\n\nTRACE = Task (goal), Request (specific ask), Action (method), Context (background), Example (reference output).\nReturn ONLY the rewritten prompt.\n\nRaw prompt:\n{{ raw_prompt }}\n{% if role %}Role: {{ role }}{% endif %}\n{% if context %}Context: {{ context }}{% endif %}\n{% if tone %}Tone: {{ tone }}{% endif %}"
}
